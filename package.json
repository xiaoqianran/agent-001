{
  "name": "agent-001",
  "private": true,
  "version": "0.1.0",
  "description": "Generative Social Simulator — runtime foundation (GOAL-001)",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "sim": "tsx apps/sim-cli/src/cli.ts",
    "observer": "tsx packages/observer/src/cli.ts",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "tsx": "^4.19.2"
  }
}
